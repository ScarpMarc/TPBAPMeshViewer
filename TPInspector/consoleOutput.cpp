#include "stdafx.h"

#include "MeshClass.h"
#include <iostream>

using namespace std;

/*void mesh::getVertexData()
{
	for (unsigned int i = 0; i < type; ++i)
	{
		for (unsigned int j = 0; j < vertices[i].size(); ++j)
		{
			cout << "Vertex " << i << " " << j << endl;
			for (unsigned int k = 0; k < vertices[i][j].other_data.size(); ++k)
			{
				cout << "DATA " << k << " " << vertices[i][j].other_data[k] << endl;
			}
		}
	}
}*/